<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:StudyNowMobileApp.ViewModels"
             xmlns:localized="clr-namespace:StudyNowMobileApp.Localization"
             mc:Ignorable="d"
             x:Class="StudyNowMobileApp.Views.ToolsMenu.ToolsPage"
             Title="{x:Static localized:LocalizedText.ToolsPageTitle}">

    <ContentPage.Content>
        <StackLayout>
            <Button x:Name="LanguageToolsBtn"
                    Text="{x:Static localized:LocalizedText.ToolsLanguageTitle}"
                    Command="{Binding NavigateToolsPageCommand}"
                    CommandParameter="{Binding Source={x:Reference LanguageToolsBtn}, Path=Text}"
                    BackgroundColor="White"
                    BorderColor="Gray"
                    BorderWidth="5"
                    CornerRadius="50"/>
        </StackLayout>
  </ContentPage.Content>
</ContentPage>